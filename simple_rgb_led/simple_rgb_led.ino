/*  This is a simple sketch for demonstating use of an RGB LED.*/int redPin = 11;int greenPin = 10;int bluePin = 9;// Common anode RGB LED means that the long pin connects to power.// Change this value if using a common cathode RGB LED.const boolean commonAnode = true;void setup() {    Serial.begin(9600);        // The LED in use is a Common Anode LED. So long pin goes to +5.    pinMode(redPin, OUTPUT);    pinMode(greenPin, OUTPUT);    pinMode(bluePin, OUTPUT);     setColor(0, 0, 0);}void loop() {  int red = random(0, 255);  int green = random(0, 255);  int blue = random(0, 255);    setColor(red, green, blue);  delay(2000);  setColor(0, 0, 0);  delay(500);}void setColor(int red, int green, int blue){  if (commonAnode) {    red = 255 - red;    green = 255 - green;    blue = 255 - blue;  }  analogWrite(redPin, red);  analogWrite(greenPin, green);  analogWrite(bluePin, blue);  }